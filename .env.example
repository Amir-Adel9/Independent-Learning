# Copy to .env and adjust. Used by docker-compose (prod + dev).

# Gateway host port for prod (default 8080).
GATEWAY_PORT=8080

# PostgreSQL (when using Docker for DB). Used by docker-compose.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=auth_dev

# Required for production API (min 32 characters).
# JWT_SECRET=your-super-secret-jwt-key-at-least-32-chars
